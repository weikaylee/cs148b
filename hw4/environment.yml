name: cs148b-hw4
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # PyTorch with CUDA 12.1 — swap 'pytorch-cuda=12.1' for 'cpuonly' if no GPU
  - pytorch>=2.4.0
  - torchvision>=0.19.0
  - pytorch-cuda=12.1
  - numpy>=1.26.0
  - matplotlib>=3.8.0
  - tqdm>=4.66.0
  - pillow>=10.0.0
  - scipy>=1.12.0
  - pyyaml>=6.0.0
  - ipykernel
  - jupyter
  - pip:
    - einops>=0.8.0
    - wandb>=0.18.0
    - torch-fidelity>=0.3.0
    - pytest>=8.0.0
    - pytest-cov>=5.0.0
